<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Medicine Detail</title>
</head>
<body>
    <% if (medicine && medicine.name) { %>
        <h2><%= medicine.name %></h2>
        <p>Price: <%= medicine.price || 'N/A' %></p>
        <p>Uses: <%= medicine.use || 'N/A' %></p>

        <% if (medicine.picture) { %>
            <% const imagePath = '/uploads/' + (medicine.picture).substring(8).replace(/\/\//g, '/') %>
            <img src="<%= imagePath %>" alt="Medicine Image">
        <% } else { %>
            <p>No picture available</p>
        <% } %>

        <!-- Render other details... -->

    <% } else { %>
        <p>No medicine data available</p>
    <% } %>
</body>
</html>
